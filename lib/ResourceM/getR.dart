
import 'package:flutter/material.dart';

class GEtR extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Get Resources'),
      ),
      body: Center(
        child: Text('This is the GEt Page'),
      ),
    );
  }
}

